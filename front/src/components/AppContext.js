import { createContext } from "react";

// CreateContext permet d'initialiser notre contexte avec des valeurs par défaut, ici ça sera pour l'id de l'utilisateur
export const uidContext = createContext();